import { CustomTable, PageWrapper } from "@/components";

const Settings = () => {
  return (
    <PageWrapper>
      <CustomTable columns={[]} />
    </PageWrapper>
  );
};

export default Settings;
